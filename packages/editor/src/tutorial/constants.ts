/**
 * When true, slab (G4) and door/opening (G5) validators always succeed (UI dev shortcut).
 * Product default is false so learners complete floor + opening.
 */
export const TUTORIAL_MINIMAL_MVP = false

/** Minimum signed polygon area (m²) for an acceptable property line */
export const TUTORIAL_MIN_SITE_AREA = 4

/** Minimum wall segments for a closed-room milestone */
export const TUTORIAL_MIN_WALLS_CLOSED_LOOP = 3
